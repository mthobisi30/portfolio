/** CSS scroll-timeline progress bar; no runtime JavaScript required. */
export default function ScrollProgress() {
  return (
    <div aria-hidden className="scroll-progress">
      <span />
    </div>
  );
}
